__pycache__/
kline_chart.png
trend_graph.png
record.csv
.vscode/settings.json
ICLR2026_QuantAgent (18).pdf
pdf_content.txt
read.html
read_files/
app/

# SQLite paper trading database (local runtime state)
paper_trades.db
paper_trades.db-shm
paper_trades.db-wal
*.db-shm
*.db-wal

# Tournament instance runtime state (per-profile DBs + heartbeats)
paper_trades_*.db
instances/*/state.json
instances/*/state.json.tmp
instances/*/overrides.json
tournament_log.json

# pytest cache
.pytest_cache/
.coverage
htmlcov/

# Vendored Kronos foundation model checkout. Cloned at setup time via
# `git clone https://github.com/shiyu-coder/Kronos kronos_forecast`.
# kronos_agent.py imports from this directory at runtime but the source
# is kept out of the repo to avoid bloating it with figures and tests.
kronos_forecast/